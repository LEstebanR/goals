import { FC } from "react";
import LoginForm from "../components/forms/loginForm";

const Login: FC = () => {
  return (
    <div className="w-60">
      <LoginForm />
    </div>
  );
};

export default Login;
